# SW(itch) ADM(in)

This is the management CLI for Oxide's rack switch.

### Contributor note

swadm is widely used across scripts and user workflows. Any changes
to the command interface must be backward compatible.

